name: Playwright Tests
on:
  workflow_call:
    inputs:
      project:
        description: 'Playwright project to run (chromium, firefox, webkit, or all)'
        type: string
        default: 'chromium'
        required: false

jobs:
  playwright-tests:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    env:
      PROJECT: ${{ inputs.project }}
    steps:
      - uses: actions/checkout@v6
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Get Playwright version
        id: playwright-version
        run: echo "version=$(pnpm ls @playwright/test --json | jq -r '.[0].devDependencies["@playwright/test"].version')" >> $GITHUB_OUTPUT
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ inputs.project }}
      - name: Install Playwright Browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: |
          if [ "$PROJECT" = "all" ]; then
            pnpm exec playwright install --with-deps
          else
            pnpm exec playwright install --with-deps "$PROJECT"
          fi
      - name: Install Playwright deps (cached browsers)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: pnpm exec playwright install-deps
      - name: Run Playwright tests
        run: |
          if [ "$PROJECT" = "all" ]; then
            pnpm exec playwright test
          else
            pnpm exec playwright test --project="$PROJECT"
          fi
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report-${{ inputs.project }}
          path: playwright-report/
          retention-days: 30
